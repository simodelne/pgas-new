name: "pgas-new"
termination: BoundedSession
topology: CyclicTopology
pure: true

preamble: |
  Program: PGAS New. You create PGAS programs through governed modes,
  explicit artifact planning, repo-curator wiring manifests, and static
  then live verification. Use only the current mode vocabulary and keep
  user decisions in the notebook-backed domain.

initial: intake_intelligence
terminal: [pr_graduation]

features:
  - base
  - reactions
  - runtime_control

channels:
  user_text: { direction: In, sync: Async }
  user_confirmation: { direction: In, sync: Async, structured_decision: true }
  system_mode_entry: { direction: In, sync: Async }
  widget_output: { direction: Out, sync: Async }
  artifact_plan_output: { direction: Out, sync: Sync }
  composite_checks_output: { direction: Out, sync: Sync }

modes:
  intake_intelligence:
    vocabulary: [record_program_target, choose_design_path, apply_default_skeleton, ask_design_question, record_q1_purpose, record_q2_entry_channel, record_q3_stages, record_q4_transitions, record_q5_delegation, record_q6_completion, record_program_intake_finalize, confirm_design, reject_design_and_revise_q1, reject_design_and_revise_q2, reject_design_and_revise_q3, reject_design_and_revise_q4, reject_design_and_revise_q5, reject_design_and_revise_q6, record_user_note, pin_notebook_note, confirm_research_scope, record_user_requested_research, web_research, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [user_text, user_confirmation, system_mode_entry, widget_output]
    preconditions:
      record_program_target:
        - { kind: FieldFalsy, path: program.target_dir_confirmed }
      choose_design_path:
        - { kind: FieldTruthy, path: program.target_dir_confirmed }
        - { kind: FieldFalsy, path: program.design_path }
      apply_default_skeleton:
        - { kind: FieldEquals, path: program.design_path, value: default }
        - { kind: FieldFalsy, path: intake.program_intake_finalized }
      record_q1_purpose:
        - { kind: FieldEquals, path: program.design_path, value: design }
        - { kind: FieldFalsy, path: intake.q1_recorded }
      record_q2_entry_channel:
        - { kind: FieldTruthy, path: intake.q1_recorded }
        - { kind: FieldFalsy, path: intake.q2_recorded }
      record_q3_stages:
        - { kind: FieldTruthy, path: intake.q2_recorded }
        - { kind: FieldFalsy, path: intake.q3_recorded }
      record_q4_transitions:
        - { kind: FieldTruthy, path: intake.q3_recorded }
        - { kind: FieldFalsy, path: intake.q4_recorded }
      record_q5_delegation:
        - { kind: FieldTruthy, path: intake.q4_recorded }
        - { kind: FieldFalsy, path: intake.q5_recorded }
      record_q6_completion:
        - { kind: FieldTruthy, path: intake.q5_recorded }
        - { kind: FieldFalsy, path: intake.q6_recorded }
      record_program_intake_finalize:
        - { kind: FieldTruthy, path: intake.q1_recorded }
        - { kind: FieldTruthy, path: intake.q2_recorded }
        - { kind: FieldTruthy, path: intake.q3_recorded }
        - { kind: FieldTruthy, path: intake.q4_recorded }
        - { kind: FieldTruthy, path: intake.q5_recorded }
        - { kind: FieldTruthy, path: intake.q6_recorded }
        - { kind: FieldFalsy, path: intake.program_intake_finalized }
      confirm_design:
        - { kind: FieldTruthy, path: intake.program_intake_finalized }
        - { kind: FieldTruthy, path: program.target_dir_confirmed }
        - { kind: FieldFalsy, path: program.design_confirmed }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: approve }
      reject_design_and_revise_q1:
        - { kind: FieldTruthy, path: intake.program_intake_finalized }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: reject }
      reject_design_and_revise_q2:
        - { kind: FieldTruthy, path: intake.program_intake_finalized }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: reject }
      reject_design_and_revise_q3:
        - { kind: FieldTruthy, path: intake.program_intake_finalized }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: reject }
      reject_design_and_revise_q4:
        - { kind: FieldTruthy, path: intake.program_intake_finalized }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: reject }
      reject_design_and_revise_q5:
        - { kind: FieldTruthy, path: intake.program_intake_finalized }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: reject }
      reject_design_and_revise_q6:
        - { kind: FieldTruthy, path: intake.program_intake_finalized }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: reject }
      web_research:
        - { kind: FieldTruthy, path: intake.user_research_authorized }
    transitions:
      - target: repo_targeting
        guard: { kind: FieldTruthy, path: program.design_confirmed }
  repo_targeting:
    vocabulary: [select_repo_target, authorize_standalone_target, load_wiring_manifest, authorize_existing_repo_target, create_curator_request, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    preconditions:
      select_repo_target:
        - { kind: FieldTruthy, path: program.target_dir_confirmed }
      authorize_standalone_target:
        - { kind: FieldTruthy, path: program.target_dir_confirmed }
        - { kind: FieldFalsy, path: repo.write_authorized }
      load_wiring_manifest:
        - { kind: FieldTruthy, path: program.target_dir_confirmed }
        - { kind: FieldFalsy, path: repo.write_authorized }
      authorize_existing_repo_target:
        - { kind: FieldEquals, path: repo.target_kind, value: existing_repo }
        - { kind: FieldEquals, path: repo.wiring_manifest.status, value: valid }
        - { kind: FieldEquals, path: repo.wiring_manifest.path, value: .pgas/wiring.yml }
      create_curator_request:
        - { kind: FieldEquals, path: repo.target_kind, value: existing_repo }
        - { kind: FieldFalsy, path: repo.write_authorized }
    transitions:
      - target: architecture_design
        guard: { kind: FieldTruthy, path: repo.write_authorized }
      - target: curator_request
        guard: { kind: FieldTruthy, path: repo.blocked }
  architecture_design:
    vocabulary: [synthesize_program_spec, design_architecture, record_user_note, web_research, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    preconditions:
      synthesize_program_spec:
        - { kind: FieldTruthy, path: program.design_confirmed }
      web_research:
        - { kind: FieldTruthy, path: intake.user_research_authorized }
    transitions:
      - target: scaffold_plan
        guard: { kind: FieldTruthy, path: program.synthesis_complete }
  scaffold_plan:
    vocabulary: [approve_artifact_plan, revise_artifact_plan, plan_artifacts, await_artifact_plan_approval, create_curator_request, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [user_confirmation, system_mode_entry, widget_output, artifact_plan_output]
    preconditions:
      plan_artifacts:
        - { kind: FieldTruthy, path: repo.write_authorized }
        - { kind: FieldTruthy, path: program.synthesis_complete }
        - { kind: FieldFalsy, path: artifact_plan.status }
        - { kind: TriggerType, triggerSet: [system_mode_entry] }
      await_artifact_plan_approval:
        - { kind: FieldTruthy, path: repo.write_authorized }
        - { kind: FieldTruthy, path: program.synthesis_complete }
        - { kind: FieldEquals, path: artifact_plan.status, value: draft }
        - { kind: FieldFalsy, path: artifact_plan.approved }
        - { kind: FieldFalsy, path: artifact_plan.awaiting_user_approval }
        - { kind: TriggerType, triggerSet: [system_mode_entry] }
      approve_artifact_plan:
        - { kind: FieldTruthy, path: repo.write_authorized }
        - { kind: FieldTruthy, path: program.synthesis_complete }
        - { kind: FieldEquals, path: artifact_plan.status, value: draft }
        - { kind: FieldFalsy, path: artifact_plan.approved }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: approve }
      revise_artifact_plan:
        - { kind: FieldTruthy, path: repo.write_authorized }
        - { kind: FieldTruthy, path: program.synthesis_complete }
        - { kind: FieldEquals, path: artifact_plan.status, value: draft }
        - { kind: FieldFalsy, path: artifact_plan.approved }
        - { kind: TriggerType, triggerSet: [user_confirmation] }
        - { kind: FieldEquals, path: inputs.user_decision.decision, value: reject }
    transitions:
      - target: domain_synthesis
        guard: { kind: FieldEquals, path: artifact_plan.status, value: approved }
  domain_synthesis:
    vocabulary: [synthesize_domain_logic, record_user_note, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    preconditions:
      synthesize_domain_logic:
        - { kind: FieldEquals, path: artifact_plan.status, value: approved }
        - { kind: FieldTruthy, path: artifact_plan.write_authorized }
        - { kind: FieldFalsy, path: program.domain_synthesis_complete }
        - { kind: TriggerType, triggerSet: [system_mode_entry] }
    transitions:
      - target: branch_write
        guard: { kind: FieldTruthy, path: program.domain_synthesis_complete }
  branch_write:
    vocabulary: [write_scaffold_artifacts, git_status, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    preconditions:
      write_scaffold_artifacts:
        - { kind: FieldEquals, path: artifact_plan.status, value: approved }
        - { kind: FieldTruthy, path: artifact_plan.write_authorized }
        - { kind: FieldTruthy, path: program.domain_synthesis_complete }
    transitions:
      - target: static_verify
        guard: { kind: FieldTruthy, path: artifacts.written }
  static_verify:
    vocabulary: [npm_install, npm_typecheck, npm_test, run_static_verification, run_parallel_static_checks, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output, composite_checks_output]
    transitions:
      - target: smoke_verify
        guard: { kind: FieldEquals, path: graduation.static_verification, value: passed }
  smoke_verify:
    vocabulary: [run_smoke_verification, confirm_live_provider_intent, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    preconditions:
      run_smoke_verification:
        - { kind: FieldEquals, path: graduation.static_verification, value: passed }
      confirm_live_provider_intent:
        - { kind: FieldEquals, path: graduation.smoke_verification, value: passed }
    transitions:
      - target: live_verify
        guard: { kind: FieldTruthy, path: graduation.ready_for_live }
  live_verify:
    vocabulary: [run_api_blackbox_verification, run_live_provider_verification, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    preconditions:
      run_api_blackbox_verification:
        - { kind: FieldTruthy, path: graduation.live_provider_intent }
      run_live_provider_verification:
        - { kind: FieldTruthy, path: graduation.live_provider_intent }
    transitions:
      - target: rebase_verify
        guard: { kind: FieldEquals, path: graduation.live_verification, value: passed }
  rebase_verify:
    vocabulary: [git_status, git_rebase_latest, run_rebase_static_verification, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    preconditions:
      run_rebase_static_verification:
        - { kind: FieldEquals, path: graduation.rebase_status, value: passed }
    transitions:
      - target: pr_graduation
        guard: { kind: FieldEquals, path: graduation.rebase_verification, value: passed }
  pr_graduation:
    vocabulary: [open_pull_request, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
  curator_request:
    vocabulary: [create_curator_request, record_user_note, session_new, session_abort_current, session_status, session_history, session_resume, session_help]
    channels: [system_mode_entry, widget_output]
    transitions:
      - target: repo_targeting
        guard: { kind: FieldTruthy, path: repo.curator_request_lodged }

proceed_to:
  authorize_standalone_target: architecture_design
  authorize_existing_repo_target: architecture_design
  design_architecture: scaffold_plan
  synthesize_program_spec: scaffold_plan
  confirm_design: repo_targeting
  approve_artifact_plan: domain_synthesis
  synthesize_domain_logic: branch_write
  write_scaffold_artifacts: static_verify
  run_static_verification: smoke_verify
  confirm_live_provider_intent: live_verify
  run_live_provider_verification: rebase_verify
  run_rebase_static_verification: pr_graduation

projection:
  intake_intelligence:
    include:
      - inputs.user_text
      - inputs.user_decision
      - inputs.user_decision.decision
      - inputs.user_decision.instruction
      - inputs.user_decision.note_mode
      - inputs.user_decision.timestamp
      - intake.mandate
      - intake.research_allowed
      - intake.research_confirmed
      - intake.user_requested_research
      - intake.purpose
      - intake.q1_recorded
      - intake.entry_channel
      - intake.q2_recorded
      - intake.stages_json
      - intake.q3_recorded
      - intake.transitions_json
      - intake.q4_recorded
      - intake.delegation_json
      - intake.q5_recorded
      - intake.completion_json
      - intake.q6_recorded
      - intake.program_intake_finalized
      - program.slug
      - program.name
      - program.target_dir
      - program.target_dir_confirmed
      - program.design_path
      - program.design_confirmed
      - program.skip_dimensions
      - notebook.entries
      - notebook.pins
    exclude: []
  repo_targeting:
    include: [program.slug, program.name, program.target_dir, program.target_dir_confirmed, repo.target_kind, repo.wiring_manifest.status, repo.wiring_manifest.path, repo.wiring_manifest_json, repo.allowed_imports, repo.write_authorized, repo.required_facilities_missing]
    exclude: []
  architecture_design:
    include: [program.slug, program.name, program.target_dir, program.runtime, program.design_confirmed, program.synthesis_complete]
    exclude: []
  scaffold_plan:
    include: [program.slug, program.name, program.target_dir, program.synthesis_complete, inputs.user_decision, inputs.user_decision.decision, inputs.user_decision.instruction, inputs.user_decision.note_mode, inputs.user_decision.timestamp, artifact_plan.status, artifact_plan.artifacts, artifact_plan.approved, artifact_plan.write_authorized, artifact_plan.awaiting_user_approval, notebook.entries, notebook.pins]
    exclude: []
  domain_synthesis:
    include: [program.slug, program.name, program.target_dir, repo.target_kind, program.synthesis_complete, program.domain_synthesis_complete, artifact_plan.status, artifact_plan.artifacts, artifact_plan.write_authorized]
    exclude: []
  branch_write:
    include: [program.slug, program.name, program.target_dir, repo.target_kind, program.synthesis_complete, program.domain_synthesis_complete, artifact_plan.status, artifact_plan.artifacts, artifact_plan.write_authorized, artifacts.written, artifacts.generated_paths]
    exclude: []
  static_verify:
    include: [program.target_dir, graduation.static_verification, graduation.static_evidence_id, graduation.composite_checks]
    exclude: []
  smoke_verify:
    include: [program.target_dir, graduation.static_verification, graduation.smoke_verification, graduation.smoke_evidence_id, graduation.live_provider_intent, graduation.ready_for_live]
    exclude: []
  live_verify:
    include: [program.target_dir, graduation.live_verification, graduation.live_provider_intent, graduation.live_evidence_id]
    exclude: []
  rebase_verify:
    include: [program.target_dir, graduation.rebase_status, graduation.rebase_evidence_id, graduation.rebase_verification, graduation.rebase_static_evidence_id]
    exclude: []
  pr_graduation:
    include: [program.target_dir, graduation.rebase_verification]
    exclude: []
  curator_request:
    include: [program.slug, repo.curator_request_lodged, repo.wiring_manifest_json, repo.required_facilities_missing, curator_requests.requests]
    exclude: []

prompts:
  intake_intelligence: |
    Output protocol (MUST follow):
    - When you need to commit state (record a Q-answer, choose a path,
      approve, etc.), emit a NATIVE OpenAI tool_call. The available tools
      are listed in the system's tools array -- their schemas define the
      exact argument names.
    - DO NOT emit a JSON object in the message content describing the
      action. DO NOT emit an object with a `MutationAction` shape. DO NOT
      use the spec's `from` + `_arg: <name>` syntax as a literal argument
      key -- that mapping marker is internal spec syntax describing how the
      engine maps your tool argument named `<name>` to a state path.
    - Every round must emit exactly one NATIVE OpenAI tool_call from the
      current mode vocabulary. Do not output free-form question text as
      assistant message content; the engine parses uncalled content as an
      invalid empty-name action.
    - To ask Q1-Q6, call `ask_design_question` with `question_number` (1..6)
      and `question_text`. The engine pauses the round; the user's typed
      reply arrives next round via inputs.user_text.
    - One tool_call per round.

    This mode collects the program-design intake. You MUST drive the design
    interview by calling the declared tools as tool calls when you are ready
    to commit governed state -- NOT by emitting raw JSON mutations.

    Per turn:
    - Process `inputs.user_text` and either call `ask_design_question` for
      the next Q1-Q6 design-interview question, OR fire the single terminal
      tool call that commits the governed state you have enough information
      to record.
    Design interview enforcement (Q1-Q6):
    - The engine enforces strict left-to-right ordering via per-question
      preconditions. You CANNOT skip ahead.
    - For each question:
      1. Call `ask_design_question` with `question_number` and
         `question_text`. The engine pauses.
      2. The user's answer arrives via user_text on the next round.
      3. Fire the corresponding `record_qN_<topic>` action with the user's
         answer parsed into that action's named argument.
      4. Move to the next question.
    - After Q6 fires, immediately fire `record_program_intake_finalize`.
    - Do NOT attempt to batch multiple answers into one action. Do NOT attempt
      to fire `record_program_intake_finalize` before all six qN_recorded flags
      are true -- the engine will reject it.
    - When you are ready to commit state: Make exactly ONE terminal tool call
      from the mode's vocabulary. The arguments to each tool are structured
      per the tool's schema; emit them as the tool's argument object, not as a
      `mutations` array. One tool call per round when committing.

    Order of intake:
    1. If `program.target_dir_confirmed` is false: call `record_program_target`
       with `{slug, name, target_dir}` derived from the user's initial message
       OR from the CLI-seeded domain (check `program.slug`, `program.name`,
       `program.target_dir` if they are pre-set).
    2. After target capture: call `choose_design_path` with `{choice: "default"}`
       or `{choice: "design"}` based on what the user asked for.
    3. If `choice = "default"`: call `apply_default_skeleton` immediately.
    4. If `choice = "design"`: ask Q1-Q6 by calling `ask_design_question`,
       recording each answer with its corresponding Q-action, then call
       `record_program_intake_finalize`.
    5. Finally call `confirm_design` to gate transition to repo_targeting.
    6. If the user_confirmation decision is `reject` and the instruction names
       Q1, Q2, Q3, Q4, Q5, or Q6, call the matching
       `reject_design_and_revise_qN` action first. Then re-run that Q-action
       and continue from there through Q6 before asking for confirmation again.

    Do not pre-fill or batch multiple tool calls in one response.
  repo_targeting: |
    Output protocol (MUST follow):
    - Emit exactly one NATIVE OpenAI tool_call from the repo_targeting vocabulary.
    - Do not emit free-form JSON action objects or multiple tool calls.

    Resolve repository write authorization before architecture_design.
    - If `program.target_dir` is set and the target is a standalone/new repo
      with no known `.pgas/wiring.yml`, call `authorize_standalone_target`.
      This action selects `repo.target_kind = standalone_repo` and sets
      `repo.write_authorized = true`.
    - If `program.target_dir` is set and the target is an existing repo with
      `.pgas/wiring.yml`, call `load_wiring_manifest` with
      `{ "repo_root": "<program.target_dir>" }`. After it succeeds, call
      `authorize_existing_repo_target`.
      A valid manifest authorizes the existing-repo path even if its
      registration strategy is `curator_request`; do not call
      `create_curator_request` for a valid manifest.
    - If `repo.target_kind` is already `standalone_repo`, call
      `authorize_standalone_target`.
    - If `repo.target_kind` is already `existing_repo`, call
      `load_wiring_manifest` first unless `repo.wiring_manifest.status` is
      already `valid`; then call `authorize_existing_repo_target`.
    - Call `create_curator_request` only when wiring is missing, invalid, or
      otherwise blocked. Do not treat the manifest's registration strategy as
      a reason to create a curator request after validation succeeds.
    - If the target kind or target directory is unclear, ask the user to
      clarify whether this is a standalone repo or an existing-repo attach
      before any write authorization.
  architecture_design: "Run the deterministic program-spec synthesizer."
  scaffold_plan: |
    Output protocol (MUST follow):
    - When you need to commit state (record a Q-answer, choose a path,
      approve, etc.), emit a NATIVE OpenAI tool_call. The available tools
      are listed in the system's tools array -- their schemas define the
      exact argument names.
    - DO NOT emit a JSON object in the message content describing the
      action. DO NOT emit an object with a `MutationAction` shape. DO NOT
      use the spec's `from` + `_arg: <name>` syntax as a literal argument
      key -- that mapping marker is internal spec syntax describing how the
      engine maps your tool argument named `<name>` to a state path.
    - Every round must emit exactly one NATIVE OpenAI tool_call from the
      current mode vocabulary. Do not output free-form prose without a
      tool_call; the engine parses uncalled content as an invalid action.
    - One tool_call per round.

    Create and approve the artifact plan before writes.
    - On system_mode_entry with no artifact_plan.status, call plan_artifacts.
    - On user_confirmation decision='approve' with artifact_plan.status='draft',
      call approve_artifact_plan. Do NOT call plan_artifacts on approval.
    - On user_confirmation decision='reject' with artifact_plan.status='draft',
      call revise_artifact_plan. Do NOT call approve_artifact_plan on rejection.
      The revision instruction in inputs.user_decision.instruction is mandatory
      feedback to preserve in the regenerated artifact list.
  domain_synthesis: "Run synthesize_domain_logic once after artifact-plan approval. This fills accepted stage bodies in transit before branch_write."
  branch_write: "Write only planned artifacts after domain_synthesis has accepted all generated bodies."
  static_verify: "Run static verification and record evidence."
  smoke_verify: "Run deterministic generated-program smoke verification before live-provider verification."
  live_verify: "Run real-provider verification through the external API."
  rebase_verify: "Rebase on latest target repo state and rerun static verification."
  pr_graduation: "Open the PR after post-rebase verification."
  curator_request: "Prepare a curator request for missing repo wiring."

ingestion:
  user_text: [inputs.user_text]
  user_confirmation:
    - inputs.user_decision
    - inputs.user_decision.decision
    - inputs.user_decision.instruction
    - inputs.user_decision.note_mode
    - inputs.user_decision.timestamp
  system_mode_entry: [inputs.mode_entry]

action_map:
  record_program_target:
    description: "Tool call for capturing program identity. Arguments must be string fields {slug, name, target_dir}; confirms program.target_dir_confirmed."
    arg_descriptions:
      slug: "Lowercase kebab-case program slug."
      name: "Human-readable program name."
      target_dir: "Repository directory where generated files should be written."
    mutations:
      - { op: MSet, path: program.slug, from_arg: slug }
      - { op: MSet, path: program.name, from_arg: name }
      - { op: MSet, path: program.target_dir, from_arg: target_dir }
      - { op: MSet, path: program.target_dir_confirmed, value: true }
    channel: widget_output
  choose_design_path:
    description: "Tool call for selecting intake path after target capture. Argument {choice} must be the string 'default' or 'design'."
    arg_descriptions:
      choice: "Either 'default' for the standard skeleton or 'design' for the six-question interview."
    mutations:
      - { op: MSet, path: program.design_path, from_arg: choice }
    channel: widget_output
  apply_default_skeleton:
    description: "Tool call with no required arguments. Populates intake.* JSON-string scalar fields with the default start->working->complete skeleton."
    mutations:
      - { op: MSet, path: intake.purpose, value: '' }
      - { op: MSet, path: intake.q1_recorded, value: true }
      - { op: MSet, path: intake.entry_channel, value: user_text }
      - { op: MSet, path: intake.q2_recorded, value: true }
      - { op: MSet, path: intake.stages_json, value: '[{"slug":"start","is_bootstrap":true},{"slug":"working"},{"slug":"complete","is_terminal":true}]' }
      - { op: MSet, path: intake.q3_recorded, value: true }
      - { op: MSet, path: intake.transitions_json, value: '[{"from":"start","to":"working","trigger":"auto"},{"from":"working","to":"complete","trigger":"auto","guard_field":"work.example_ready","guard_value":true}]' }
      - { op: MSet, path: intake.q4_recorded, value: true }
      - { op: MSet, path: intake.delegation_json, value: '{}' }
      - { op: MSet, path: intake.q5_recorded, value: true }
      - { op: MSet, path: intake.completion_json, value: '{"final_stage":"complete","guard_field":"work.example_ready"}' }
      - { op: MSet, path: intake.q6_recorded, value: true }
      - { op: MSet, path: intake.program_intake_finalized, value: true }
    channel: widget_output
  ask_design_question:
    description: "Ask the user a single design-interview question (Q1-Q6). Pauses the round; the next round's inputs.user_text carries the answer."
    arg_descriptions:
      question_number: "Which Q is being asked (1-6)."
      question_text: "The question prompt to display to the user."
    mutations:
      - { op: MSet, path: intake.last_question_asked, from_arg: question_number }
      - { op: MSet, path: intake.last_question_text, from_arg: question_text }
    channel: widget_output
  record_q1_purpose:
    description: "Capture the user's answer to Q1 (program purpose). One short paragraph describing what the program does."
    arg_descriptions:
      purpose: "User's answer to Q1: the program purpose."
    mutations:
      - { op: MSet, path: intake.purpose, from_arg: purpose }
      - { op: MSet, path: intake.q1_recorded, value: true }
    channel: widget_output
  record_q2_entry_channel:
    description: "Capture Q2 (entry channel -- how does work arrive: user_text, webhook, scheduled tick, another program delegating)."
    arg_descriptions:
      entry_channel: "User's answer to Q2: how work enters the program."
    mutations:
      - { op: MSet, path: intake.entry_channel, from_arg: entry_channel }
      - { op: MSet, path: intake.q2_recorded, value: true }
    channel: widget_output
  record_q3_stages:
    description: "Capture Q3 (stage list -- the named modes the program transitions through)."
    arg_descriptions:
      stages_json: |
        Provide a JSON-encoded value. The strict format is required for
        downstream synthesis.

        Valid examples:
        - Array (Q3 stages_json): "[\"intake\", \"analysis\", \"complete\"]"
        - Rich array with per-stage domain specs:
          "[{\"slug\":\"intake\",\"is_bootstrap\":true},{\"slug\":\"analysis\",\"domain_spec\":{\"reads\":[\"inputs.initial_user_text\"],\"produces\":{\"result_json\":{\"stage\":\"string\",\"decision\":\"string\"},\"items_json\":[\"decision:<decision>\"]},\"rules\":[\"Use only recorded input facts.\"],\"invariants\":[\"result_json.stage must equal analysis.\"]}},{\"slug\":\"complete\",\"is_terminal\":true}]"

        If the user's Q3 answer contains objects, domain_spec, rules,
        invariants, or produces schemas, preserve those objects exactly. Do
        not compress rich stage objects into a bare array of names.

        Do NOT HTML-escape the string. Use raw " characters, not &quot;.

        The foundry accepts JSON5-style input as a fallback (unquoted keys,
        bare identifiers), but emits canonical JSON in governed state.
    mutations:
      - { op: MSet, path: intake.stages_json, from_arg: stages_json }
      - { op: MSet, path: intake.q3_recorded, value: true }
    channel: widget_output
  record_q4_transitions:
    description: "Capture Q4 (transition list -- how stages advance, branch, loop, or guard)."
    arg_descriptions:
      transitions_json: |
        Provide a JSON-encoded value. The strict format is required for
        downstream synthesis.

        Valid example:
        - Array (Q4 transitions_json): "[{\"from\":\"intake\",\"to\":\"analysis\",\"guard_field\":\"intake.ready\"}]"

        Do NOT HTML-escape the string. Use raw " characters, not &quot;.

        The foundry accepts JSON5-style input as a fallback (unquoted keys,
        bare identifiers), but emits canonical JSON in governed state.
    mutations:
      - { op: MSet, path: intake.transitions_json, from_arg: transitions_json }
      - { op: MSet, path: intake.q4_recorded, value: true }
    channel: widget_output
  record_q5_delegation:
    description: "Capture Q5 (delegation design -- whether and how work delegates)."
    arg_descriptions:
      delegation_json: |
        Provide a JSON-encoded value. The strict format is required for
        downstream synthesis.

        Valid example:
        - No explicit per-stage override: "{}" or "{\"enabled\": false}".
        - Raw "none" is also accepted and is normalized to "{\"enabled\": false}".
        - Optional per-stage execution model:
          "{\"stages\":{\"scope_definition\":{\"kind\":\"llm-reasoning\",\"reasoning_per_turn\":true},\"fee_modelling\":{\"kind\":\"pure-compute\"},\"crm_lookup\":{\"kind\":\"external-adapter\"}}}"
        - Legacy direct per-stage kind control is still accepted:
          "{\"scope_definition\":{\"kind\":\"llm-reasoning\",\"target\":\"define_scope\"},\"fee_modelling\":{\"kind\":\"pure-compute\",\"target\":\"compute_fee_model\"}}"

        Use kind when the user explicitly says a stage is deterministic,
        LLM reasoning, or an external adapter. Valid kind values include
        llm-reasoning, pure-compute, and external-adapter. Use
        reasoning_per_turn=true only when a stage must run agent reasoning on
        each turn.

        Do NOT HTML-escape the string. Use raw " characters, not &quot;.

        The foundry accepts JSON5-style input as a fallback (unquoted keys,
        bare identifiers), but emits canonical JSON in governed state.
    mutations:
      - { op: MSet, path: intake.delegation_json, from_arg: delegation_json }
      - { op: MSet, path: intake.q5_recorded, value: true }
    channel: widget_output
  record_q6_completion:
    description: "Capture Q6 (completion criteria -- terminal stage and completion guard)."
    arg_descriptions:
      completion_json: |
        Provide a JSON-encoded value. The strict format is required for
        downstream synthesis.
        Preserve every user-provided top-level key exactly, including
        blocked_stage, blocked_guard_field, and outputs. Do not shorten or
        summarize a richer completion object to only final_stage/guard_field.

        Valid example:
        - Object (Q6 completion_json): "{\"final_stage\": \"complete\", \"guard_field\": \"work.done\"}"
        - Rich object (Q6 completion_json): "{\"final_stage\":\"complete\",\"guard_field\":\"delivery.finalized\",\"blocked_stage\":\"blocked\",\"outputs\":{\"proposal_html\":\"string\",\"docx_export_ready\":\"boolean\"}}"
        - Generic collection lifecycle (Q6 completion_json): "{\"final_stage\":\"complete\",\"guard_field\":\"work_units.all_terminal\",\"collection_lifecycle\":{\"version\":1,\"name\":\"work_units\",\"item_label\":\"work unit\",\"storage\":{\"items_path\":\"work_units.items_json\",\"event_path\":\"work_units.pending_event_json\",\"violation_path\":\"work_units.lifecycle_violation_json\"},\"item\":{\"id_field\":\"id\",\"status_field\":\"status\",\"schema\":{\"id\":\"string\",\"title\":\"string\",\"status\":\"string\"}},\"statuses\":[{\"name\":\"pending\",\"initial\":true},{\"name\":\"reviewed\",\"terminal\":true},{\"name\":\"removed\",\"terminal\":true}],\"transitions\":[{\"from\":\"pending\",\"to\":\"reviewed\",\"stage\":\"review_work\",\"action\":\"mark_reviewed\",\"managed_by\":\"llm\"}],\"aggregate\":{\"guard_field\":\"work_units.all_terminal\",\"terminal_statuses\":[\"reviewed\",\"removed\"],\"require_non_empty\":true}}}"

        collection_lifecycle is optional. When provided, keep it generic:
        neutral names such as items, work_units, or reviewable_records; no
        domain-specific templates. The aggregate.guard_field is the completion
        guard the synthesized program uses for the final transition.

        Do NOT HTML-escape the string. Use raw " characters, not &quot;.

        The foundry accepts JSON5-style input as a fallback (unquoted keys,
        bare identifiers), but emits canonical JSON in governed state.
    mutations:
      - { op: MSet, path: intake.completion_json, from_arg: completion_json }
      - { op: MSet, path: intake.q6_recorded, value: true }
    channel: widget_output
  record_program_intake_finalize:
    description: "Final commit of the design interview once all 6 Q-answers are recorded. Idempotent. The LLM should call this immediately after record_q6_completion fires."
    mutations:
      - { op: MSet, path: intake.program_intake_finalized, value: true }
    channel: widget_output
  confirm_design:
    description: "Tool call after explicit user approval. Confirms the proposed mode and transition list before repo_targeting."
    mutations:
      - { op: MSet, path: program.design_confirmed, value: true }
    channel: widget_output
  reject_design_and_revise_q1:
    description: "Reopen design intake from Q1 onward after explicit confirm_design rejection."
    mutations:
      - { op: MSet, path: intake.q1_recorded, value: false }
      - { op: MSet, path: intake.q2_recorded, value: false }
      - { op: MSet, path: intake.q3_recorded, value: false }
      - { op: MSet, path: intake.q4_recorded, value: false }
      - { op: MSet, path: intake.q5_recorded, value: false }
      - { op: MSet, path: intake.q6_recorded, value: false }
      - { op: MSet, path: intake.program_intake_finalized, value: false }
      - { op: MSet, path: program.design_confirmed, value: false }
    channel: widget_output
  reject_design_and_revise_q2:
    description: "Reopen design intake from Q2 onward after explicit confirm_design rejection."
    mutations:
      - { op: MSet, path: intake.q2_recorded, value: false }
      - { op: MSet, path: intake.q3_recorded, value: false }
      - { op: MSet, path: intake.q4_recorded, value: false }
      - { op: MSet, path: intake.q5_recorded, value: false }
      - { op: MSet, path: intake.q6_recorded, value: false }
      - { op: MSet, path: intake.program_intake_finalized, value: false }
      - { op: MSet, path: program.design_confirmed, value: false }
    channel: widget_output
  reject_design_and_revise_q3:
    description: "Reopen design intake for Q3 after explicit confirm_design rejection while preserving later recorded answers."
    mutations:
      - { op: MSet, path: intake.q3_recorded, value: false }
      - { op: MSet, path: program.design_confirmed, value: false }
    channel: widget_output
  reject_design_and_revise_q4:
    description: "Reopen design intake from Q4 onward after explicit confirm_design rejection."
    mutations:
      - { op: MSet, path: intake.q4_recorded, value: false }
      - { op: MSet, path: intake.q5_recorded, value: false }
      - { op: MSet, path: intake.q6_recorded, value: false }
      - { op: MSet, path: intake.program_intake_finalized, value: false }
      - { op: MSet, path: program.design_confirmed, value: false }
    channel: widget_output
  reject_design_and_revise_q5:
    description: "Reopen design intake from Q5 onward after explicit confirm_design rejection."
    mutations:
      - { op: MSet, path: intake.q5_recorded, value: false }
      - { op: MSet, path: intake.q6_recorded, value: false }
      - { op: MSet, path: intake.program_intake_finalized, value: false }
      - { op: MSet, path: program.design_confirmed, value: false }
    channel: widget_output
  reject_design_and_revise_q6:
    description: "Reopen design intake from Q6 onward after explicit confirm_design rejection."
    mutations:
      - { op: MSet, path: intake.q6_recorded, value: false }
      - { op: MSet, path: intake.program_intake_finalized, value: false }
      - { op: MSet, path: program.design_confirmed, value: false }
    channel: widget_output
  record_user_note: { description: "Record a notebook note.", mutations: [{ op: MAppend, path: notebook.entries, from_arg: note }], channel: widget_output }
  pin_notebook_note: { description: "Pin a notebook note for next-turn projection.", mutations: [{ op: MAppend, path: notebook.pins, from_arg: key }], channel: widget_output }
  confirm_research_scope:
    description: "Confirm discretionary web research for the current mandate."
    mutations:
      - { op: MSet, path: intake.research_confirmed, value: true }
      - { op: MSet, path: intake.research_allowed, value: true }
      - { op: MSet, path: intake.user_research_authorized, value: true }
    channel: widget_output
  record_user_requested_research:
    description: "Record user-requested research as authorized."
    mutations:
      - { op: MSet, path: intake.user_requested_research, value: true }
      - { op: MSet, path: intake.research_allowed, value: true }
      - { op: MSet, path: intake.user_research_authorized, value: true }
      - { op: MAppend, path: research.queries, from_arg: query }
    channel: widget_output
  web_research: { description: "Run user-confirmed web research.", mutations: [], channel: widget_output }
  session_new: { description: "Control-plane new session command.", mutations: [], channel: widget_output }
  session_abort_current: { description: "Control-plane abort current session command.", mutations: [], channel: widget_output }
  session_status: { description: "Control-plane status command.", mutations: [], channel: widget_output }
  session_history: { description: "Control-plane history command.", mutations: [], channel: widget_output }
  session_resume: { description: "Control-plane resume command.", mutations: [], channel: widget_output }
  session_help: { description: "Control-plane help command.", mutations: [], channel: widget_output }
  select_repo_target:
    description: "Select standalone or existing-repo target."
    arg_descriptions:
      target_kind: "Either 'standalone_repo' for a new generated repo or 'existing_repo' for attachment to a repo with .pgas/wiring.yml."
    mutations:
      - { op: MSet, path: repo.target_kind, from_arg: target_kind }
      - { op: MSet, path: repo.write_authorized, value: false }
      - { op: MSet, path: repo.blocked, value: false }
    channel: widget_output
  authorize_standalone_target:
    description: "Authorize standalone repo writes after target selection."
    mutations:
      - { op: MSet, path: repo.target_kind, value: standalone_repo }
      - { op: MSet, path: repo.write_authorized, value: true }
      - { op: MSet, path: repo.blocked, value: false }
      - { op: MSet, path: repo.wiring_manifest.status, value: not_required }
    channel: widget_output
  load_wiring_manifest:
    description: "Load and validate fixed .pgas/wiring.yml for existing-repo writes."
    arg_descriptions:
      repo_root: "The absolute path to the existing repository root. Read this from program.target_dir in the current state."
    mutations:
      - { op: MSet, path: repo.target_kind, value: existing_repo }
      - { op: MSet, path: repo.wiring_manifest.repo_root, from_arg: repo_root }
      - { op: MSet, path: repo.wiring_manifest.status, value: valid }
      - { op: MSet, path: repo.wiring_manifest.path, value: .pgas/wiring.yml }
      - { op: MSet, path: repo.blocked, value: false }
      - { op: MSet, path: repo.write_authorized, value: true }
    channel: widget_output
  authorize_existing_repo_target:
    description: "Authorize existing-repo writes after fixed manifest validation."
    mutations:
      - { op: MSet, path: repo.write_authorized, value: true }
    channel: widget_output
  create_curator_request:
    description: "Prepare a repo curator request."
    arg_descriptions:
      message: "Additional optional context to include in the curator request body."
      repo_root: "Optional override for the repository root; normally read from repo.wiring_manifest.repo_root or program.target_dir."
      slug: "Optional program slug override for the curator request artifact name."
      title: "Optional curator request title. Defaults from program.name or program.slug."
      body: "Optional curator request body. Defaults from program identity, repo root, and message."
    mutations: [{ op: MSet, path: repo.curator_request_lodged, value: true }]
    channel: widget_output
  design_architecture:
    description: "Mark architecture ready."
    mutations: [{ op: MSet, path: program.architecture_ready, value: true }]
    channel: widget_output
  synthesize_program_spec:
    description: "Run the mechanical synthesizer (no LLM call). Writes the spec to in-process transit; flips program.synthesis_complete."
    channel: widget_output
    mutations:
      - { op: MSet, path: program.synthesis_complete, value: true }
  plan_artifacts:
    description: |
      INTERNAL: Auto-fires exactly ONCE on system_mode_entry into scaffold_plan
      to draft the artifact plan. Do NOT call this in response to /approve —
      the user-driven approval action is approve_artifact_plan. Its precondition
      forbids re-firing after artifact_plan.status is set.
    result_path: artifact_plan.artifacts
    mutations:
      - { op: MSet, path: artifact_plan.status, value: draft }
      - { op: MSet, path: artifact_plan.approved, value: false }
      - { op: MSet, path: artifact_plan.write_authorized, value: false }
      - { op: MSet, path: artifact_plan.awaiting_user_approval, value: false }
    channel: artifact_plan_output
  await_artifact_plan_approval:
    description: |
      INTERNAL: After an artifact plan is drafted, duplicate system_mode_entry
      continuations must call this no-write action and wait for explicit user
      confirmation. Do NOT call approve_artifact_plan unless the trigger is
      user_confirmation with decision='approve'.
    mutations:
      - { op: MSet, path: artifact_plan.awaiting_user_approval, value: true }
    channel: widget_output
  approve_artifact_plan:
    description: |
      USER-DRIVEN: Fires when the user types /approve to authorize the
      drafted artifact plan. This is the ONLY tool to call in scaffold_plan
      when receiving inputs.user_decision.decision='approve'. Sets
      artifact_plan.status='approved' and triggers the write_scaffold_artifacts
      transition.
    mutations:
      - { op: MSet, path: artifact_plan.status, value: approved }
      - { op: MSet, path: artifact_plan.approved, value: true }
      - { op: MSet, path: artifact_plan.write_authorized, value: true }
    channel: widget_output
  revise_artifact_plan:
    description: |
      USER-DRIVEN: Fires when the user types /reject in scaffold_plan to
      revise the drafted artifact plan. This is the ONLY tool to call in
      scaffold_plan when receiving inputs.user_decision.decision='reject'.
      Re-runs deterministic artifact planning, preserves user-required
      artifacts from intake/notebook/rejection feedback, and keeps the plan
      unapproved until a later /approve.
    result_path: artifact_plan.artifacts
    mutations:
      - { op: MSet, path: artifact_plan.status, value: draft }
      - { op: MSet, path: artifact_plan.approved, value: false }
      - { op: MSet, path: artifact_plan.write_authorized, value: false }
    channel: artifact_plan_output
  synthesize_domain_logic:
    description: |
      Run per-stage domain-logic synthesis against the frozen generated
      contracts. The handler stores accepted stage bodies (and the full
      stage-local audit) in the synthesizer transit store; the handler's
      summary payload (status, stage_count, audit, spec sha delta) is
      presented on widget_output.
    mutations:
      - { op: MSet, path: program.domain_synthesis_complete, value: true }
    # widget_output (with NO result_path) is REQUIRED for auto-continuation:
    # the engine's NoticeContinuation consumer only publishes notice_emitted
    # (and thus the system_mode_entry auto_continuation trigger) for
    # EffectActions on the widget_output channel, and the engine's ER-2
    # compiler check forbids result_path on an Async channel. On the former
    # domain_synthesis_output (Sync) channel the domain_synthesis ->
    # branch_write transition stalled forever in live sessions (2026-07-03
    # two-lane UAT: a/b/c stalled after synthesize_domain_logic in 9/9
    # failing attempts). This mirrors Phase 3.26's architecture: explicit
    # notice continuations via widget_output for deterministic actions.
    channel: widget_output
  write_scaffold_artifacts: { description: "Write planned artifacts.", mutations: [{ op: MSet, path: artifacts.written, value: true }], channel: widget_output }
  git_status: { description: "Read git status.", mutations: [], channel: widget_output }
  npm_install: { description: "Install dependencies.", mutations: [], channel: widget_output }
  npm_typecheck: { description: "Run typecheck.", mutations: [], channel: widget_output }
  npm_test: { description: "Run tests.", mutations: [], channel: widget_output }
  run_static_verification:
    description: "Record static verification evidence."
    mutations:
      - { op: MSet, path: graduation.static_verification, from_arg: status }
      - { op: MSet, path: graduation.static_evidence_id, from_arg: evidence_id }
      - { op: MSet, path: graduation.static_verification_recorded, value: true }
    channel: widget_output
  run_parallel_static_checks:
    description: |
      OPTIONAL parallel fan-out. Packs independent static checks (import
      boundary, declared-mode terminal reachability, evidence shape) into ONE
      action whose children run CONCURRENTLY and aggregate into a single
      combined envelope at graduation.composite_checks. The single-call
      npm_typecheck / npm_test / run_static_verification actions remain the
      DEFAULT — only pack here when you deliberately want the parallel combined
      result. Provide the inputs to check under the action payload
      (imports[], modes[], evidence{status,evidence_id}); the envelope's
      `status` is succeeded when every child passes and partial when any child
      fails, with per-child results under `children`.
    result_path: graduation.composite_checks
    mutations: []
    channel: composite_checks_output
  run_smoke_verification:
    description: "Run deterministic generated-program smoke verification and record evidence."
    mutations:
      - { op: MSet, path: graduation.smoke_verification, from_arg: status }
      - { op: MSet, path: graduation.smoke_evidence_id, from_arg: evidence_id }
      - { op: MSet, path: graduation.smoke_verification_recorded, value: true }
    channel: widget_output
  confirm_live_provider_intent:
    description: "Confirm user-selected live-provider graduation after static verification."
    mutations:
      - { op: MSet, path: graduation.live_provider_intent, value: true }
      - { op: MSet, path: graduation.ready_for_live, value: true }
    channel: widget_output
  run_api_blackbox_verification: { description: "Run black-box API verification.", mutations: [], channel: widget_output }
  run_live_provider_verification:
    description: "Record live provider verification evidence."
    mutations:
      - { op: MSet, path: graduation.live_verification, from_arg: status }
      - { op: MSet, path: graduation.live_evidence_id, from_arg: evidence_id }
      - { op: MSet, path: graduation.live_verification_recorded, value: true }
    channel: widget_output
  git_rebase_latest:
    description: "Rebase on latest target repo state and record rebase status."
    mutations:
      - { op: MSet, path: graduation.rebase_status, from_arg: status }
      - { op: MSet, path: graduation.rebase_evidence_id, from_arg: evidence_id }
      - { op: MSet, path: graduation.rebase_recorded, value: true }
    channel: widget_output
  run_rebase_static_verification:
    description: |
      Record post-rebase static verification evidence AND reconcile the generated
      graduation audit artifact from governed graduation state (#100). The handler
      rewrites the audit markdown's evidence rows to actual statuses/ids so a
      terminal pr_graduation does not ship an audit that still reads "pending".
    mutations:
      - { op: MSet, path: graduation.rebase_verification, from_arg: status }
      - { op: MSet, path: graduation.rebase_static_evidence_id, from_arg: evidence_id }
      - { op: MSet, path: graduation.rebase_static_verification_recorded, value: true }
    channel: widget_output
  open_pull_request: { description: "Open pull request.", mutations: [], channel: widget_output }

reactions:
  debug_approve_artifact_plan_preconditions:
    event: AfterIngestion
    watch:
      - repo.write_authorized
      - program.synthesis_complete
      - artifact_plan.status
      - artifact_plan.approved
      - inputs.user_decision.decision
    write_scope:
      - debug.approve_artifact_plan_preconditions_checked
  normalize_intake_json_fields:
    event: AfterMutation
    watch:
      - intake.q3_recorded
      - intake.q4_recorded
      - intake.q5_recorded
      - intake.q6_recorded
    write_scope:
      - intake.stages_json
      - intake.transitions_json
      - intake.delegation_json
      - intake.completion_json
  normalize_static_verification_status:
    event: AfterMutation
    watch:
      - graduation.static_verification_recorded
    write_scope:
      - graduation.static_verification
  normalize_smoke_verification_status:
    event: AfterMutation
    watch:
      - graduation.smoke_verification_recorded
    write_scope:
      - graduation.smoke_verification
  normalize_live_verification_status:
    event: AfterMutation
    watch:
      - graduation.live_verification_recorded
    write_scope:
      - graduation.live_verification
  normalize_rebase_status:
    event: AfterMutation
    watch:
      - graduation.rebase_recorded
    write_scope:
      - graduation.rebase_status
  normalize_rebase_static_verification_status:
    event: AfterMutation
    watch:
      - graduation.rebase_static_verification_recorded
    write_scope:
      - graduation.rebase_verification

schema:
  inputs.user_text: string
  inputs.user_decision: object
  inputs.user_decision.decision: string
  inputs.user_decision.instruction: string
  inputs.user_decision.note_mode: string
  inputs.user_decision.timestamp: string
  inputs.mode_entry: object
  inputs.mode_entry.mode: string
  inputs.mode_entry.from_mode: string
  inputs.mode_entry.entry_round: number
  intake.mandate: string
  intake.last_question_asked: number
  intake.last_question_text: string
  intake.purpose: string
  intake.q1_recorded: boolean
  intake.entry_channel: string
  intake.q2_recorded: boolean
  intake.stages_json: string
  intake.q3_recorded: boolean
  intake.transitions_json: string
  intake.q4_recorded: boolean
  intake.delegation_json: string
  intake.q5_recorded: boolean
  intake.completion_json: string
  intake.q6_recorded: boolean
  intake.program_intake_finalized: boolean
  intake.research_confirmed: boolean
  intake.user_requested_research: boolean
  intake.research_allowed: boolean
  intake.user_research_authorized: boolean
  notebook.entries: array
  notebook.pins: array
  research.queries: array
  research.completed: boolean
  repo.target_kind: string
  repo.blocked: boolean
  repo.write_authorized: boolean
  repo.curator_request_lodged: boolean
  repo.wiring_manifest.status: string
  repo.wiring_manifest.path: string
  repo.wiring_manifest.repo_root: string
  repo.wiring_manifest_json: string
  repo.allowed_imports: array
  repo.allowed_imports.*: string
  repo.required_facilities_missing: array
  program.runtime: string
  program.slug: string
  program.name: string
  program.design_path: string
  program.design_confirmed: boolean
  program.synthesis_complete: boolean
  program.domain_synthesis_complete: boolean
  program.target_dir: string
  program.target_dir_confirmed: boolean
  program.skip_dimensions: array
  program.skip_dimensions.*: string
  program.architecture_ready: boolean
  artifact_plan.status: string
  artifact_plan.artifacts: array
  artifact_plan.artifacts.*: object
  artifact_plan.approved: boolean
  artifact_plan.write_authorized: boolean
  artifact_plan.awaiting_user_approval: boolean
  artifacts.written: boolean
  artifacts.generated_paths: array
  curator_requests.requests: array
  graduation.static_verification: string
  graduation.static_verification_recorded: boolean
  graduation.static_evidence_id: string
  graduation.smoke_verification: string
  graduation.smoke_verification_recorded: boolean
  graduation.smoke_evidence_id: string
  graduation.live_verification: string
  graduation.live_verification_recorded: boolean
  graduation.live_provider_intent: boolean
  graduation.ready_for_live: boolean
  graduation.live_evidence_id: string
  graduation.rebase_status: string
  graduation.rebase_recorded: boolean
  graduation.rebase_evidence_id: string
  graduation.rebase_verification: string
  graduation.rebase_static_verification_recorded: boolean
  graduation.rebase_static_evidence_id: string
  graduation.composite_checks: object
  debug.approve_artifact_plan_preconditions_checked: boolean
  governance.round_counter: number

repair_bound: 2

fallback:
  channel: widget_output
  payload: { ok: false }

guidance:
  intake_intelligence:
    - "Every round must emit exactly one native tool_call from the current mode vocabulary. Do NOT output free-form question text as assistant message content; the engine parses uncalled content as an invalid empty-name action."
    - "If program.target_dir_confirmed is not true: first capture program identity. The CLI may have pre-filled program.slug, program.name, or program.target_dir via initialDomain. If slug is provided but name is missing, derive name as title-case of slug. If target_dir is blank, default to './<slug>'. When slug/name/target_dir are known, call record_program_target with slug, name, and target_dir."
    - "After record_program_target, if program.design_path is not set, call choose_design_path with choice='design' when the user asked to design the program or choice='default' when the user asked for the default skeleton."
    - "If program.design_path == 'default' and intake.program_intake_finalized is not true: immediately call apply_default_skeleton. Do NOT ask Q1-Q6."
    - "Design interview enforcement (Q1-Q6): The engine enforces strict left-to-right ordering via per-question preconditions. You CANNOT skip ahead. For each question: 1. Call ask_design_question with question_number (1..6) and question_text. The engine pauses. 2. The user's answer arrives via user_text on the next round. 3. Fire the corresponding record_qN_<topic> action with the user's answer parsed into that action's named argument. 4. Move to the next question. After Q6 fires, immediately fire record_program_intake_finalize. Do NOT attempt to batch multiple answers into one action. Do NOT attempt to fire record_program_intake_finalize before all six qN_recorded flags are true -- the engine will reject it."
    - "If program.design_path == 'design' and intake.program_intake_finalized is not true: ask the 6 design questions IN ORDER, one question per round, with brief context for each. Call ask_design_question for the question round; wait for the user's answer in inputs.user_text before recording exactly that answer via the next record_qN_<topic> action. Order matters."
    - "Q1 Purpose — one sentence on what the program does. Default if skipped: '<program-name>-driven workflow agent'."
    - "Q2 Entry channel — how does work arrive? (user_text, webhook, scheduled tick, another program delegating). Default if skipped: 'user_text'."
    - "Q3 Stages of work — distinct stages, named in order; these become the mode names. Default if skipped: ['start', 'working', 'complete']. If the user's answer contains rich stage objects with domain_spec, rules, invariants, or produces schemas, pass that exact rich object array as stages_json; do not simplify it to a string array."
    - "Q4 Decision points — branches, loops, bail-outs; these become extra transitions, optionally with guards. Default if skipped: no extra transitions."
    - "Q5 Delegation — does any stage delegate to a child session / another program, or need an explicit per-stage execution model? Default if skipped: 'none'. Use {\"stages\":{\"<slug>\":{\"kind\":\"llm-reasoning\"|\"pure-compute\"|\"external-adapter\",\"reasoning_per_turn\":true|false}}} only when the user gives explicit per-stage execution intent; otherwise keep the legacy no-delegation shapes like {}, {\"enabled\":false}, or none."
    - "Q6 Completion criteria — how do you know the program is done? Default if skipped: terminal mode = last stage, guard = 'work.example_ready'."
    - "If the user replies 'skip' to any Q1-Q6 dimension, record the skip in program.skip_dimensions and use the dimension's default in the JSON-string scalar fields."
    - "For Q3-Q6, the *_json arguments must be JSON-string scalar fields, not nested arrays or objects."
    - "After intake is finalized via apply_default_skeleton OR record_program_intake_finalize, echo back the proposed mode list + transitions + target_dir as plain text (intent='confirm_design') and instruct the user to type /approve or /reject. Wait for the user_confirmation trigger."
    - "When you need user approval (e.g., before confirm_design or approve_artifact_plan), instruct the user to type /approve or /reject (with optional follow-up instruction). The REPL routes these to the user_confirmation channel with structured payload. Do NOT accept plain text replies as approvals."
    - "On user_confirmation with decision='approve', call confirm_design."
    - "On user_confirmation with decision='reject' and instruction naming Q1, Q2, Q3, Q4, Q5, or Q6, first call the matching reject_design_and_revise_qN action (for example reject_design_and_revise_q3 for Q3). Re-ask/record that Q and then re-emit the confirm_design echo-back; preserve later recorded answers unless the user explicitly asks to revise them."
    - "Don't re-ask anything you already extracted from the user's free-text introduction."
  repo_targeting:
    - "This mode is mandatory between confirm_design and architecture_design; do not skip it."
    - "If program.target_dir is set and the target is standalone/no .pgas/wiring.yml is known, call authorize_standalone_target. That action sets repo.target_kind='standalone_repo' and repo.write_authorized=true."
    - "If program.target_dir points to an existing repo with .pgas/wiring.yml, call load_wiring_manifest with repo_root=program.target_dir, then call authorize_existing_repo_target after the manifest status is valid."
    - "A VALID .pgas/wiring.yml routes to authorize_existing_repo_target/proceed even when registration.strategy is curator_request. create_curator_request is only for missing/invalid wiring or an explicit repo blocker."
    - "Only leave repo_targeting after repo.write_authorized is true; the transition to architecture_design is guarded on that field."
  scaffold_plan:
    - "Only call plan_artifacts on system_mode_entry to produce the artifact plan from the synthesized spec in in-process transit after repo.write_authorized and program.synthesis_complete are true and artifact_plan.status is still unset. If the handler reports the spec is missing from transit, call synthesize_program_spec again; the synthesizer is mechanical and idempotent."
    - "If a later system_mode_entry arrives while artifact_plan.status='draft' and artifact_plan.approved=false, call await_artifact_plan_approval and wait. Do NOT call approve_artifact_plan on system_mode_entry."
    - "Echo the artifact-plan list (paths + purposes) to the user as plain text and instruct them to type /approve or /reject."
    - "When you need user approval (e.g., before confirm_design or approve_artifact_plan), instruct the user to type /approve or /reject (with optional follow-up instruction). The REPL routes these to the user_confirmation channel with structured payload. Do NOT accept plain text replies as approvals."
    - "On user_confirmation decision='approve' with artifact_plan.status='draft' and artifact_plan.approved false, call approve_artifact_plan. Do NOT call plan_artifacts on an approval trigger. Then the transition to domain_synthesis fires."
    - "On user_confirmation decision=reject with artifact_plan.status='draft' and artifact_plan.approved false, call revise_artifact_plan. Do NOT call approve_artifact_plan on a rejection. The revised artifact plan must preserve explicit user-required paths from inputs.user_decision.instruction, notebook notes, and the intake dossier, then stay in scaffold_plan for a later /approve."
  domain_synthesis:
    - "On system_mode_entry after artifact_plan.status='approved', call synthesize_domain_logic exactly once. This runs per-stage generate/verify/repair against the frozen contracts and hard-fails instead of producing stubs."
    - "Do not call write_scaffold_artifacts from this mode. Wait for program.domain_synthesis_complete to transition to branch_write."
  architecture_design:
    - "On entering architecture_design after confirm_design, call synthesize_program_spec. The handler performs mechanical synthesis with no LLM call, writes the YAML to in-process transit for this session, and flips program.synthesis_complete via the action-map mutation."
    - "After synthesize_program_spec succeeds, transition to scaffold_plan. If the foundry server restarted and downstream planning reports missing transit state, re-run synthesize_program_spec; same intake produces the same YAML."

control_plane:
  version: 1
  session:
    program: "pgas-new"
    binding: identity_channel_program
    concurrency:
      policy: single_active
      on_busy: reject_with_status
  free_text:
    control: ask
    bind:
      query: "$text"
  controls:
    ask:
      title: Ask
      aliases: ["/ask"]
      args:
        query:
          type: string
          required: true
          capture: rest
      dispatch:
        - op: create_session
          as: session
          program: "pgas-new"
          domain_context:
            query: "$args.query"
        - op: trigger
          session: "$session.id"
          channel: user_text
          payload: "$args.query"
          mode: async
    approve_artifact_plan:
      title: Approve current gate
      aliases: ["/approve"]
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: approve
          mode: async
      presentation:
        priority: primary
    revise_artifact_plan:
      title: Reject and revise artifact plan
      aliases: ["/revise-plan"]
      args:
        instruction:
          type: string
          required: false
          capture: rest
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: reject
            instruction: "$args.instruction"
          mode: async
      presentation:
        priority: contextual
    confirm_design:
      title: Confirm design intake
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: approve
          mode: async
      presentation:
        priority: contextual
    reject_design_and_revise_q1:
      title: Reject and revise Q1
      aliases: ["/reject-q1"]
      args:
        instruction:
          type: string
          required: true
          capture: rest
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: reject
            instruction: "$args.instruction"
          mode: async
      presentation:
        priority: contextual
    reject_design_and_revise_q2:
      title: Reject and revise Q2
      aliases: ["/reject-q2"]
      args:
        instruction:
          type: string
          required: true
          capture: rest
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: reject
            instruction: "$args.instruction"
          mode: async
      presentation:
        priority: contextual
    reject_design_and_revise_q3:
      title: Reject and revise Q3
      aliases: ["/reject-q3"]
      args:
        instruction:
          type: string
          required: true
          capture: rest
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: reject
            instruction: "$args.instruction"
          mode: async
      presentation:
        priority: contextual
    reject_design_and_revise_q4:
      title: Reject and revise Q4
      aliases: ["/reject-q4"]
      args:
        instruction:
          type: string
          required: true
          capture: rest
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: reject
            instruction: "$args.instruction"
          mode: async
      presentation:
        priority: contextual
    reject_design_and_revise_q5:
      title: Reject and revise Q5
      aliases: ["/reject-q5"]
      args:
        instruction:
          type: string
          required: true
          capture: rest
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: reject
            instruction: "$args.instruction"
          mode: async
      presentation:
        priority: contextual
    reject_design_and_revise_q6:
      title: Reject and revise Q6
      aliases: ["/reject-q6"]
      args:
        instruction:
          type: string
          required: true
          capture: rest
      availability:
        when: "active_session"
      dispatch:
        - op: trigger
          session: "$active_session.id"
          channel: user_confirmation
          payload:
            decision: reject
            instruction: "$args.instruction"
          mode: async
      presentation:
        priority: contextual
    abort:
      title: Abort run
      aliases: ["/abort", "/stop"]
      availability:
        when: "active_session.running"
      dispatch:
        - op: abort_session
          session: "$active_session.id"
      presentation:
        priority: destructive
        confirm: true
    new:
      title: New session
      aliases: ["/new", "/reset", "/restart"]
      dispatch:
        - op: create_session
          as: session
          program: "pgas-new"
      presentation:
        priority: secondary
    history:
      title: Recent sessions
      aliases: ["/history", "/recent"]
      dispatch:
        - op: list_sessions
          as: sessions
      presentation:
        priority: secondary
    status:
      title: Status
      aliases: ["/status"]
      availability:
        when: "active_session"
      dispatch:
        - op: render_session
          session: "$active_session.id"
          view: status
      presentation:
        priority: contextual
    resume:
      title: Resume
      aliases: ["/resume"]
      availability:
        when: "active_session"
      dispatch:
        - op: send_runtime_control
          session: "$active_session.id"
          control: resume
      presentation:
        priority: contextual
    help:
      title: Help
      aliases: ["/help"]
      dispatch:
        - op: render_help
      presentation:
        priority: secondary
