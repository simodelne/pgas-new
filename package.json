{
  "name": "claude-pgas-plugin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin for scaffolding pgas consumers and programs",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run --config tests/vitest.config.ts",
    "test:legacy": "bash tests/plugin-manifest.test.sh && bash tests/template-render.test.sh && bash tests/auth-scaffold.test.sh && bash tests/server-typecheck.test.sh && bash tests/spec-load.test.sh && bash tests/program-smoke.test.sh",
    "test": "npm run typecheck && npm run test:unit",
    "pgas-new": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "js-yaml": "^4.1.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
