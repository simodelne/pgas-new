# CODEOWNERS for {{CONSUMER_NAME}}
#
# Per the orchestrator + sub-agents model: the curator (human) reviews
# behavioral-risk surfaces. Sub-agents open PRs against any path; the
# orchestrator approves; the curator is auto-requested on the surfaces
# below.

# Engine boundary — never edited from this repo, but flag any
# attempt for review.
node_modules/@simodelne/pgas-*/    @{{GH_OWNER}}

# Spec / program / handler logic — the load-bearing program shape.
/programs/                          @{{GH_OWNER}}
/server/                            @{{GH_OWNER}}

# Auth (will become load-bearing in Brief 2)
/server/auth/                       @{{GH_OWNER}}

# Governance lockfiles (if present)
/governance/                        @{{GH_OWNER}}

# QC framework
/qc/                                @{{GH_OWNER}}

# Architecture docs
/audit/                             @{{GH_OWNER}}

# Plugin marker comments — changes here break the plugin's injection
# protocol; require explicit review.
/docs/MARKER-PROTOCOL.md            @{{GH_OWNER}}
