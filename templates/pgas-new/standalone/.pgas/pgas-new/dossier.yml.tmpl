program:
  slug: "{{SLUG}}"
  name: "{{NAME}}"
  runtime: typescript-node
notebook:
  purpose: "Durable intake notes and user ideas for {{NAME}}."
research:
  user_confirmation_required: true
graduation:
  static_verification: pending
  live_provider_round_trip: pending
