{
  "name": "{{SLUG}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/server.ts",
    "repl": "node --import tsx src/repl/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --pool=threads --maxWorkers=1"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.1",
    "@simodelne/pgas-server": "^{{PGAS_SERVER_VERSION}}",
    "chalk": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
