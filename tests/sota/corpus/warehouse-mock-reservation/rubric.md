# Warehouse Mock Reservation Rubric

- Uses the declared in-memory catalog and marks the lookup as in_memory_mock.
- Does not call a network or invent inventory outside the catalog.
- Carries lookup availability into the reservation decision without losing requested order facts.
